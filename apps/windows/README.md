# CDMS Windows App

## Run in development

```powershell
cd apps\windows
npm install
npm start
```

## Build Windows installer

```powershell
cd apps\windows
npm run dist
```

The desktop app launches the existing backend automatically and opens the CDMS portal in an Electron window.
